version: '3.8'

services:
  postgres:
    image: postgres:17
    container_name: multiplayer-soccer-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-football_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "127.0.0.1:5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-football_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7
    container_name: multiplayer-soccer-redis
    ports:
      - "127.0.0.1:6379:6379" # opcional: mapeia para host local apenas
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    image: multiplayer-soccer-app:latest
    container_name: multiplayer-soccer-app
    # AJUSTE 2: Permitir que o Node abra milhares de sockets
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    environment:
      DB_POOL_SIZE: 200     # Aumentamos o pool do Node
      DB_CONN_TIMEOUT: 10000 # Timeout de conexão com o banco
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-football_db}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      REDIS_HOST: redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      JWT_SECRET: ${JWT_SECRET:-''}
      PORT: 3000
    # For local development you might want:
    # volumes:
    #   - ./:/usr/src/app
    expose:
      - "3000"
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  nginx:
    image: nginx:stable-alpine # Use a imagem oficial, não precisa do Dockerfile customizado agora
    container_name: multiplayer-soccer-nginx
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    ports:
      - "80:80"
    volumes:
      # AQUI ESTÁ A MÁGICA:
      # Substituímos a configuração global inteira pelo nosso arquivo
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
